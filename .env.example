# Google Apps Script Web App URL for waitlist submissions (server-side only)
# This is used by the Vercel serverless function, NOT exposed to the browser
GOOGLE_APPS_SCRIPT_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
